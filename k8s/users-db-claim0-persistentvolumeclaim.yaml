apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: users-db-claim0
  name: users-db-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
