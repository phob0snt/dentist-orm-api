apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: lead-db-claim0
  name: lead-db-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
